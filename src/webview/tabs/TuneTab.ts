// src/webview/tabs/TuneTab.ts

export function TuneTab() {
    const container = document.createElement('div');
    container.className = 'tab-content tune-tab';
    container.innerHTML = `<p>Tune Tab Content (empty for now)</p>`;
    return container;
  }
  